# Your EVM wallet private key (with 0x prefix)
# Must have Base Sepolia USDC for payment
EVM_PRIVATE_KEY=0x...

# Optional: override default API URL and path
# RESOURCE_SERVER_URL=http://localhost:8080
# ENDPOINT_PATH=/api/weather